'use strict';

const firstNum = parseInt(prompt('Enter first number'));
const secondNum = parseInt(prompt('Enter second number'));

if(firstNum > secondNum) {
  console.log('First number greater than the second number');
} else {
  console.log('Second number greater than the first number');
};
